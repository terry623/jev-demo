{
  "name": "jev-pixel-lab",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/pixels.js && node --check src/composition.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
