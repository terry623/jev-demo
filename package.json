{
  "name": "jev-little-world",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/decisions.js && node --check public/world.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
